# Security

Do not commit `.env`, API keys, raw resumes, processed candidate profiles, runtime indexes, or logs containing candidate information.

The repository `.gitignore` excludes the private data/runtime paths used by HireFlow. If you discover a security issue in a deployed instance, report it privately to the deployment owner rather than opening a public issue containing secrets or candidate data.
