# Contributing

1. Create a feature branch.
2. Do not add raw candidate data or secrets.
3. Add tests for behavior changes.
4. Run `make test`, `make lint`, and `make validate-release`.
5. Document any new model/ranking assumptions and evaluation limitations.

Changes that alter ranking logic should include a reproducible evaluation comparison rather than only qualitative examples.
